# 大商所期货数据爬虫开发文档

## 📋 项目概述

本文档记录了大商所（大连商品交易所）期货数据爬虫的开发过程，包括问题分析、技术挑战、解决方案和最终实现。

## 🎯 需求描述

用户需要爬取大商所网站的两类数据：
1. **持仓排名数据** - 期货公司的成交量、持买单量、持卖单量排名
2. **持仓限额数据** - 各品种期货合约的最大持仓限制

## 🔍 技术挑战分析

### 1. 反爬虫机制

大商所网站使用了严格的反爬虫措施：

| 方法 | 测试结果 | 原因 |
|------|----------|------|
| requests库 | ❌ 412错误 | 服务器拒绝自动化请求 |
| Playwright | ❌ 空页面 | 网站检测到自动化工具 |
| Selenium+headless | ❌ 空页面 | 检测到无头浏览器 |
| undetected-chromedriver | ✅ 成功 | 有效绕过检测 |

### 2. 网络限制

```bash
# HTTP测试
curl http://www.dce.com.cn/
# 结果: 412 Precondition Failed

# HTTPS测试  
curl https://www.dce.com.cn/
# 结果: 连接被拒绝
```

### 3. 页面结构复杂性

- 主页面返回空HTML（39字符）
- 实际数据在iframe中
- 需要访问特定的数据URL

## ✅ 解决方案

### 核心技术：undetected-chromedriver

```python
import undetected_chromedriver as uc
from selenium.webdriver.chrome.options import Options

# 必须使用非headless模式
options = Options()
options.add_argument('--no-sandbox')
options.add_argument('--disable-dev-shm-usage')
options.add_argument('--window-size=1920,1080')

driver = uc.Chrome(options=options, version_main=151)
```

### 关键发现：直接访问iframe URL

```python
# 错误的方法（返回空页面）
url = "http://www.dce.com.cn/publicweb/businessguidelines/queryContractInfo.html"

# 正确的方法（访问数据iframe）
url = "http://www.dce.com.cn/frontend/dcereport/#/zh/memberDealPosiQuotes?tradeType=1&variety=i"
```

### 关键参数

- `tradeType=1` - 期货类型
- `variety=i` - 品种代码（i=铁矿石）
- 其他品种代码：a=豆一, m=豆粕, j=焦炭等

## 📁 最终实现

### 文件结构

```
/home/liyuexuan/下载/
├── 大商所持仓爬虫.py          # 专用爬虫（持仓排名）
├── 通用大商所爬虫.py          # 通用爬虫（任意页面）
├── 使用说明.txt              # 用户指南
├── 持仓限额爬虫指南.txt       # 限额数据指南
├── 运行爬虫.sh               # 一键运行脚本
└── dce_data/                 # 数据输出目录
    ├── 铁矿石持仓数据.json
    ├── 铁矿石持仓数据.xlsx
    └── 页面截图.png
```

### 程序1：大商所持仓爬虫.py

**功能**: 专门爬取铁矿石持仓排名数据

**关键代码**:
```python
def crawl_dce_position_data():
    # 访问铁矿石持仓数据页面
    url = "http://www.dce.com.cn/frontend/dcereport/#/zh/memberDealPosiQuotes?tradeType=1&variety=i"
    driver.get(url)
    time.sleep(15)  # 等待页面加载
    
    # 查找并解析表格
    tables = driver.find_elements(By.TAG_NAME, "table")
    for table in tables:
        df = pd.read_html(StringIO(table.get_attribute('outerHTML')))[0]
        if len(df) > 10:  # 主数据表
            # 保存数据
            result = {
                "品种": "铁矿石",
                "数据": df.to_dict('records')
            }
```

### 程序2：通用大商所爬虫.py

**功能**: 可爬取任意大商所页面的表格数据

**关键代码**:
```python
def crawl_dce_page(url, output_name):
    driver.get(url)
    time.sleep(15)
    
    # 提取所有表格
    tables = driver.find_elements(By.TAG_NAME, "table")
    all_data = []
    
    for table in tables:
        df = pd.read_html(StringIO(table.get_attribute('outerHTML')))[0]
        all_data.append({
            "table_index": i,
            "data": df.to_dict('records')
        })
    
    # 保存为JSON和Excel
    with pd.ExcelWriter(excel_file) as writer:
        for i, table_info in enumerate(all_data):
            df = pd.DataFrame(table_info["data"])
            df.to_excel(writer, sheet_name=f'表格{i+1}')
```

## 📊 数据格式

### 持仓排名数据结构

```json
{
  "品种": "铁矿石",
  "品种代码": "i",
  "爬取时间": "2026-08-14 17:34:08",
  "总记录数": 21,
  "数据": [
    {
      "0": "1",
      "1": "国泰君安（代客）",
      "2": 30622,    // 成交量
      "3": -11149,   // 成交增减
      "4": "1",
      "5": "国泰君安（代客）",
      "6": 20502,    // 持买单量
      "7": -1488,    // 买单增减
      "8": "1",
      "9": "国泰君安（代客）",
      "10": 20352,   // 持卖单量
      "11": 1626     // 卖单增减
    }
  ]
}
```

## 🔧 部署说明

### 环境要求

```bash
# Python 3.14+
pip install selenium undetected-chromedriver pandas openpyxl
```

### 使用方法

**爬取铁矿石持仓数据**:
```bash
cd /home/liyuexuan/下载
python 大商所持仓爬虫.py
```

**爬取其他页面**:
```bash
python 通用大商所爬虫.py "目标URL" "输出文件名"
```

## ❌ 未解决的问题：持仓限额数据

### 问题分析

经过全面测试，持仓限额数据在以下情况中未找到：

1. **公开网页测试失败**:
   ```
   ❌ http://www.dce.com.cn/dce/channel/242.html (限仓参数)
   ❌ http://www.dce.com.cn/dce/channel/241.html (风控参数)
   ❌ http://www.dce.com.cn/dce/channel/244.html (品种细则)
   ```

2. **页面加载超时**: 这些页面可能存在JavaScript加载问题

3. **数据格式限制**: 限额数据可能在PDF文档中或需要登录访问

### 可能的解决方案

1. **手动查找**: 用户需要在浏览器中手动找到正确的URL
2. **API申请**: 申请大商所API访问权限
3. **PDF解析**: 如果数据在PDF中，需要开发PDF解析功能
4. **登录认证**: 如果需要登录，需要实现认证流程

## 🎯 关键经验总结

### 技术要点

1. **反爬虫绕过**: undetected-chromedriver是最有效的解决方案
2. **非headless模式**: 必须使用，headless会被检测
3. **直接URL访问**: 避开主页面的反爬虫，直接访问数据iframe
4. **等待时间**: 页面加载需要15-20秒
5. **表格解析**: pandas.read_html是处理HTML表格的最佳方法

### 调试技巧

```python
# 检查页面是否加载成功
if len(driver.page_source) > 1000:
    print("页面加载成功")
else:
    print("页面为空，可能被检测")

# 查找所有表格
tables = driver.find_elements(By.TAG_NAME, "table")
print(f"找到 {len(tables)} 个表格")

# 保存页面截图用于调试
driver.save_screenshot("debug.png")
```

## 📝 后续改进建议

1. **支持更多品种**: 添加品种代码配置
2. **批量爬取**: 实现多品种批量爬取
3. **数据存储**: 添加数据库存储功能
4. **定时任务**: 实现定时自动爬取
5. **持仓限额**: 继续寻找限额数据源或API接口

## 🔄 版本信息

- **开发日期**: 2026-08-14
- **Python版本**: 3.14
- **Chrome版本**: 151.0.7922.75
- **主要依赖**: 
  - undetected-chromedriver (最新版)
  - selenium (最新版)
  - pandas (最新版)
  - openpyxl 3.1.5

## 📞 技术支持

如遇到问题，请检查：
1. Chrome浏览器是否正确安装
2. 网络连接是否正常
3. 大商所网站是否可访问
4. Python依赖是否完整安装

---

**文档状态**: ✅ 完成
**最后更新**: 2026-08-14 17:45
**开发环境**: Linux 7.1.6 + Python 3.14 + Claude Code
